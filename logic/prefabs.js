class Prefabs {
    // keep around for complex prefab components
/*
    static createGate(grid, x, y, fn, numInputs) {
        return new Gate(grid, x, y, fn, numInputs);
    }

    static createPortLeft(grid, x, y, name) {
        return new Port(grid, x, y, name, 'left');
    }

    static createPortRight(grid, x, y, name) {
        return new Port(grid, x, y, name, 'right');
    }
*/
}